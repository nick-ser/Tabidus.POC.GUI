<UserControl x:Class="Tabidus.POC.GUI.UserControls.Software.UpdateSourceSchedulingElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
 
        <Grid VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding IsSelected}" Command="{Binding ChangeUpdateSourceSchedulingCommand}"></CheckBox>
        <TextBlock Grid.Column="1" Text="{Binding UpdateSourceName}" Foreground="{StaticResource textblockColorCodeBrush}" VerticalAlignment="Center" FontSize="10" FontFamily="{DynamicResource ResourceKey=OpenSansRegular}"></TextBlock>
        </Grid>

</UserControl>
